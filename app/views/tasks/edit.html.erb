<%= form_for(@task) do |f| %>

    <%= f.label :tilte %>
    <%= f.text_field :title %>
    <%= f.label :details %>
    <%= f.text_field :details %>
    <%= f.submit %>
    <%= check_box_tag(:completed) %>
    <%= label_tag(:complete, "Task complete") %>
<% end %>

<%= link_to "Back to the list", tasks_path %>